/* ============================================================================
 * @deprecated Prefer `useWorkspace` — kept as a thin alias for composer callers.
 * ========================================================================== */
export { default, normalizeWorkspace, invalidateWorkspaceQueries } from './useWorkspace';
