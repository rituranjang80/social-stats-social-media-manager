/* ============================================================================
 *  Module rail UI retired — navigation lives in FeatureSidebar.jsx.
 *  Kept as a no-op so existing imports do not break.
 * ========================================================================== */
export default function ModuleRail() {
  return null;
}
