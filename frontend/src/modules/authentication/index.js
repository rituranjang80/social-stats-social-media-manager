/**
 * @module authentication
 * Public authentication module — login UI and session hooks.
 */
export { default as LoginPage } from '../../pages/LoginPage';
export { AuthProvider, useAuth } from '../../hooks/useAuth';
