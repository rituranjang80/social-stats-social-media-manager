# Backend core

Shared cross-cutting utilities will live here as the refactor progresses (logging helpers, base service classes, generic repository patterns).

**Phase 1:** Django continues to use `social_stats` as the installed app; do not duplicate models.
